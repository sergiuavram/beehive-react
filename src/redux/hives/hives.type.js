export const hivesTypes = {
    CREATE_HIVE: 'CREATE_HIVE',
    GET_HIVES: 'GET_HIVES', //this are the hives names from the hives table to use for /hives
    GET_HIVE: 'GET_HIVE', // this is for individual hive, to populate it with required data
}